<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body>
    <% for (var url in urls) {%>
    <li><%= url + ' : ' + urls[url] %></li>
    <% } %>
<!--     <li><%- JSON.stringify(urls) %></li>
 -->   </body>
</html>